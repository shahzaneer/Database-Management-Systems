use org;

select * from worker;

select * from worker order by salary; -- order by us the keywird jisko use kr k hum krte hain SORTING
-- yeh by default ascending order main hoti hai

select * from worker order by salary ASC;  -- same effect as of above 
select * from worker order by salary DESC;  --  descending order main show karega
